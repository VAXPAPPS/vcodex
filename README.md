```sh
meson setup build
meson compile -C build
meson install -C build
```
sudo apt-get update && sudo apt-get install -y libgtk-3-dev libgtksourceview-4-dev libvte-2.91-dev libsoup-3.0-dev libjson-glib-dev libgit2-glib-1.0-dev libsecret-1-dev